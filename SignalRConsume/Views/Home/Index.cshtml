@{
    ViewData["Title"] = "Home Page";
}

@* @section Scripts{
    <script src="~/lib/microsoft/signalr/dist/browser/signalr.min.js"></script>
    
    <script type="text/javascript">
        $(document).ready(() => {
            var connection = new signalR.HubConnectionBuilder().withUrl("http://localhost:7802/VisitorHub").build();
            $("#connState").text(connection.connectionState);

            connection.start().then(() => {
                $("#connState").text(connection.connectionState);
            }).catch((err) => { console.log(err) });
        });
    </script>
}

<div class="container">
    <br />
    <div class="alert alert-warning">
        Bağlantı Durumu : <b id="connState"></b>
    </div>
</div> *@
